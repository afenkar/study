# SQL 练习（对照 BUSINESS 表，脱敏填写）

> 把真实表名改成练习名即可，例如 `t_news`、`t_user`。  
> 目标：5 条能手写、能讲 JOIN 方向、能说该不该加索引。

假设官网常见关系（按你库改）：

- 用户 `user(id, name, ...)`  
- 文章 `news(id, title, author_id, created_at)`  
- 分类 `category(id, name)`  
- 文章-分类 若是多对多再加中间表

---

## 题 1 · 条件查询

列出最近 10 条已发布新闻的标题和时间。

```sql
-- 你的答案：

```

## 题 2 · INNER JOIN

新闻标题 + 作者名（没有作者的不要）。

```sql


```

## 题 3 · LEFT JOIN

所有分类，以及每个分类下的文章数（没有文章的分类也要出现，计数为 0）。

```sql


```

## 题 4 · 分页 + 排序

某分类下新闻，按时间倒序，第 2 页，每页 10 条。

```sql


```

## 题 5 · 索引思路（不用写对，要写「为什么」）

`news` 经常按 `author_id` 和 `created_at` 查。你加什么索引？`EXPLAIN` 里希望看到什么？

```text
索引：
EXPLAIN 期望：
```

---

## 口试题（C7–8）

1. INNER 和 LEFT 差在哪？  
2. `WHERE` 里对列用 `DATE(created_at) = '2026-01-01'` 为什么可能用不上索引？  
3. 主键索引和普通索引你项目里各用在哪？
